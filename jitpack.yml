# jitpack.yml
jdk:
  - openjdk17

# Собираем только API модуль для публикации
# (plugin модуль не нужен в зависимостях других проектов)
install: mvn clean install -DskipTests

# Указываем что используем multi-module проект
before_install:
  - sdk install java 17.0.9-tem
  - sdk use java 17.0.9-tem